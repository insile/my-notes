##### DataFrame 属性
```python
DataFrame.columns
	# DataFrame 的列标签
DataFrame.index
	# DataFrame 的行索引(行标签)
DataFrame.values
	# 返回存储值的 ndarray 
DataFrame.dtypes
	# 返回存储每个列数据类型的 Series
DataFrame.shape
	# 返回一个表示 DataFrame 维度的元组
DataFrame.size
	# 返回一个表示此对象中元素数量的 int
```