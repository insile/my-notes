##### 隐函数定理
- 隐函数定理
	- 隐函数定理描述了在某些条件下, 隐式定义的方程可以解出一个显式函数
		- 设 $F(x, y) = 0$ 是一个关于 $x \in \mathbb{R}^n$ 和 $y \in \mathbb{R}^m$ 的隐式方程, 且 $F$ 在 $(x_0, y_0)$ 处可微. 如果雅可比矩阵 $\frac{\partial F}{\partial y}(x_0, y_0)$ 是非奇异的, 则存在 $y = g(x)$, 使得 $F(x, g(x)) = 0$, 并且 $g(x)$ 是连续可微的
