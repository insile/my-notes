##### 仿射变换
- 仿射变换
	- 仿射变换是一种保留直线和平行关系的几何变换, 但不一定保留距离和角度, 仿射变换对一个向量空间进行一次线性变换并接上一个平移, 变换为另一个向量空间
		- $\mathbb{R}^n$ 中的仿射变换是[[映射]] $T:\mathbb{R}^n\to\mathbb{R}^n$ , 满足 $T(\pmb{x}) = R\pmb{x} + t$, 其中, $R$ 是一个[[逆矩阵|可逆矩阵]], 表示旋转, 反射, 缩放, 剪切等线性变换, $t$ 是平移向量

