##### 欧氏变换
- 欧氏变换
	- 欧式变换, 也称刚性变换或等距变换, 是保持距离和角度不变的变换, 包括旋转, 平移和反射, 对一个向量空间进行一次正交变换并接上一个平移, 变换为另一个向量空间
		- $\mathbb{R}^n$ 中的欧式变换是[[映射]] $T:\mathbb{R}^n\to\mathbb{R}^n$ , 满足 $T(\pmb{x}) = R\pmb{x} + t$, 其中, $R$ 是一个[[正交矩阵]], 表示旋转或反射等正交变换, $t$ 是平移向量

