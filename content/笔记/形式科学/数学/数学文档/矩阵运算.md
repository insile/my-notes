##### 矩阵运算
- 矩阵运算
	- 矩阵加法 $A\pm B$
		- 同型矩阵对应位置的元素加减
			- $A \pm B=B\pm A$
			- $(A+B)+B=A+(B+ C)$
	- 矩阵数乘  $k A$
		- 用一个数 $k$ 乘矩阵 $A$ 即与所有元素相乘
			- $k(A+B)=kA+kB$
			- $(k+l)A=kA+lA$
			- $(kl)A=k(lA)=l(kA)$
			- $1A=A$, $-1A=\mathbf{-A}$, $0A=\mathbf{0}$
	- 矩阵乘法 $AB= C$
		- 矩阵 $C$ 元素 $c_{ij}$ 为 $A$ 的 $i$ 行元素与 $B$ 的 $j$ 列元素对应的乘积之和, 俗称前行乘后列, 只有第一个矩阵 $A$ (左边的矩阵)的列数与第二个矩阵 $B$ (右边的矩阵)的行数相等时两个矩阵才能相乘
			- $(AB) C=A(B C)$
			- $A(B+ C)=AB+A C$
			- $k(AB)=A(kB)=(kA)B$
			- $\mathbf{E}A=A\mathbf{E}=A$
			- $\mathbf{0}A=A\mathbf{0}=\mathbf{0}$
		- $\begin{bmatrix} 3 & -1 & 1\\ 2 & 2 & 0 \end{bmatrix}\mathbf{} \begin{bmatrix} 1 & -1 \\ 1 & 1 \\ 2 & 1 \end{bmatrix}\mathbf{}=\begin{bmatrix}4 & -3\\4 &0\end{bmatrix}$
	- 方阵方幂 $A^k$
		- 规定对非零方阵 $A$ 有 $A^0=\mathbf{E}$
			- $A^k=AA...A$
	- 矩阵转置 $A^T$
		- 矩阵元素位置行列号互换, 即沿主对角线翻转,
			- $(A^T)^T=A$
			- $(A+B)^T=A^T+B^T$
			- $(AB)^T=B^TA^T$
			- $(kA)^T=kA^T$
			- $(A^{-1})^T = (A^T)^{-1}$
		- $\begin{bmatrix} 1 & 2 & 3\\ 4 & 5 & 6\\ 7 & 8 &9\end{bmatrix}^T=\begin{bmatrix} 1 & 4 & 7\\ 2 & 5 & 8\\ 3 & 6 &9\end{bmatrix}$

