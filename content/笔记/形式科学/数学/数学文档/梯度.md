##### 梯度
- 梯度
	- 可微多元函数 $f$ 的梯度是向量函数 $\nabla f$, 在一点的梯度是以该点[[偏导数]]为坐标分量, 梯度指向函数增长最快的方向, 梯度的大小表示在该方向上的最大变化率
		- 以二元函数 $f(x,y)$ 为例, 函数 $f$ 在点 $(x_0,y_0)$ 的梯度为 $\displaystyle\nabla{f}=\frac{\partial{f}}{\partial{x}}\pmb{i}+\frac{\partial{f}}{\partial{y}}\pmb{j}$
