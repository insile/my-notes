##### 空语句
- `空语句`
	- 空语句由分号（;）组成，表示即使 JavaScript 语法要求提供语句，也不执行任何语句。相反的情况是，你希望有多个语句，但 JavaScript 只允许一个语句，可以使用块语句来实现，它将多个语句组合成一个语句。
```js
;



const arr = [1, 2, 3];

// 将所有数组值赋值为 0
for (let i = 0; i < arr.length; arr[i++] = 0) /* 空语句 */ ;

console.log(arr);
// [0, 0, 0]

```

