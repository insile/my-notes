##### 主函数
- 主函数
	- 每个 C 程序都有必须命名为 `main` 的主函数. 主函数充当程序执行的起点. 它通常通过将调用定向到程序中的其他函数来控制程序执行, 所以有一些限制适用于主函数, 而不适用于任何其他[[C.函数|函数]]
- 语法
```c
int main (void) { body }
// 不含参数
int main (int argc, char *argv[]) { body }
// 含参数
```
