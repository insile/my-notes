##### 注释
- 注释
	- 注释为了帮助读者理解程序, 编译器会忽略它们, 可将注释放在任意的地方
- 语法
	- `/* 注释内容, 可以多行 */`
	- `// 注释内容, 只能单行`
- 示例
```c
#include <stdio.h>
// 预处理 
int main()
// 主函数
{
    printf("hello, world\n");
    // 打印输出
    return 0;
    // 返回值
}
```