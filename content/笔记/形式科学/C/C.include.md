##### `#include`
- `#include`
	- `#include`是文件包含[[C.预处理器|预处理器指令]], 主要用来引入对应的[[C.头文件|头文件]]. 处理过程很简单, 就是将头文件的内容插入到该命令所在的位置, 从而把头文件和当前源文件连接成一个源文件, 这与复制粘贴的效果相同

```c
// 形式一 #include <>
#include <stdio.h> // 加载系统提供的文件, 查找系统目录

// 形式二 #include ""
#include "fibo.h" // 加载用户提供的文件, 查找当前工作目录
```


