##### 条件判断符
- `<` 小于; `>` 大于; `<=` 小于等于; `>=` 大于等于; `==` 等于; `!=` 不等于
- [[in]] 包含; [[is]] 相同; [[not]] 非; [[and]] 与; [[or]] 或
```python
# `<` 小于
1<2  # True

# `>` 大于
1>2  # False

# `<=` 小于等于
1<=2  # True

# `>=` 大于等于
1>=2  # False

# `==` 等于
1==2  # False

# `!=` 不等于
1!=2  # False
```