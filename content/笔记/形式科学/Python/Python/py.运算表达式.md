##### 运算表达式
- 运算表达式
	- 运算表达式是由[[py.标识符|标识符]]和[[py.符号|运算符]]组成的表达式, 用于表示计算过程并生成一个值, 下面一些示例
```python
age = 25  # 赋值年龄

age = age + 2  # 算术表达式

is_adult = age >= 18  # 比较运算，判断年龄是否大于等于 18

is_middle_aged = age < 50 and age > 30 # 布尔运算
```
