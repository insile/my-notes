##### git commit
- `git commit`
	- 简介
		- 提交暂存区更改到 `.git` 目录, 每次提交都是对项目当前状态的快照, 并附有提交信息描述此次更改的目的
	- 选项
		- `-m <msg>` 
			- 在命令行中直接添加提交信息
		- `-a`
			- 暂存然后提交, 包括删改
		- `--amend` 
			- 修改上一次提交
	- 示例
```shell
git commit -m "Initial commit"
# 提交暂存区的更改并添加提交信息
```
