##### git add
- `git add`
	- 简介
		- 将工作树更改添加到暂存区, 标记为下一步提交的一部分, 即暂存提交的更改. 在提交之前可以多次执行此命令, 它仅添加所指定的内容
	- 选项
		- `<pathspec>` 
			- 指定添加文件路径
		- `–-all` 
			- 添加工作树所有更改内容, 包括增删改
		- `.` 
			- 添加当前目录所有更改内容, 包括增删改
	- 示例
```shell
git add –-all
git add .
# 在仓库顶层目录两者作用相同
```
