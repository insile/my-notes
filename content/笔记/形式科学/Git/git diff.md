##### git diff
- `git diff`
	- 简介
		- 比较工作树, 暂存区和版本库之间的不同, 它可以显示哪些行发生了变化, 以及这些变化的具体内容
	- 选项
		- `--staged`
			- 比较暂存区与上次提交的文件
		- `<commit>` `​<commit>`
			- 比较两个不同提交的文件
		- `<branch> <branch>`
			- 比较两个不同分支的文件
	- 示例
```shell
git diff
# 默认比较工作树和暂存区的差异
```
